# 達芬奇 DaVinci Resolve 21 AI 自動對拍電影感剪輯系統
## 📘 專案藍圖與開發者技術手冊

本專案是一套高效能的 **AI 智慧音訊對拍與電影感視訊自動剪接系統**。該系統直接與 **DaVinci Resolve 21** 的 Python API 進行互動，自動將原始相機素材依照背景音樂的鼓點暫態進行對拍，並以極致精準的節奏自動剪接至時間軸上，完美解決了影格率轉換相位差、訊號處理延遲、音軌裁切偏移、劇烈抖動廢片、視覺運鏡方向衝突以及品牌 Logo 結尾包裝等專業剪輯痛點。

---

## 🌟 核心特色與演算法架構

### 1. AI 鏡頭動態導演 (AI Camera Motion Director)
* **起承轉合 4 階段鏡頭包裝模型**：利用達芬奇 API 的 `SetProperty()`，對時間軸片段進行動態比例與旋轉包裝：
  1. **【起】0s ~ 5s (環境與後台準備 — Setup)**：平穩大氣開場。維持 `Zoom = 1.0`, `Rotation = 0.0`。
  2. **【承】5s ~ 12s (美髮造型與產品特寫 — Detail)**：產品特寫強烈推近。自動套用 `Zoom = 1.05`, `Rotation = 0.0`，聚焦造型細節。
  3. **【轉】12s ~ 20.8s (名模走秀高潮 — Catwalk Climax)**：斜切手持卡點。套用 `Zoom = 1.12`，並在重拍剪點上**交替進行 `3.5°` 與 `-3.5°` 的左右傾斜**！創造出極具視覺震撼力的時尚手持卡點效果。
  4. **【合】20.8s ~ 25.0s (觀眾掌聲與謝幕定格 — Finale)**：品牌定格大推近。自動套用 `Zoom = 1.18`, `Rotation = 0.0`，強力鎖定最終品牌瓶身與觀眾掌聲。
* **一秒啟用動態縮放 (Dynamic Zoom)**：全選時間軸片段 (`Ctrl + A`)，在右上角檢查器點亮 **「動態縮放 (Dynamic Zoom)」**，即可完美結合鼓點卡點與平滑推拉動畫。

### 2. 電影感運動平滑與連續留線 (Motion Flow Smoothing)
* **理想運動能量包絡線 (Motion Envelope)**：對全片拍點的理想運動度套用 **5 拍滑動平均濾波器 (5-Beat Moving Average Filter)**，將鋸齒跳變平滑化為溫和起伏的正弦能量波。
* **前後幀動態留線銜接防禦**：計算當前片段與前一鏡片段的運動能量絕對差值。一旦差值 **`> 3.0`**，則給予 **`0.15` 的遞增重罰**，強迫 AI 優先挑選具備視覺物理慣性（Visual Inertia）的流暢影片，確保視覺舒適度。

### 3. 高精度暫態鼓點檢測與 climax 對齊
* 使用 **FFmpeg** 將 any 音樂格式秒級轉碼為標準 Mono WAV（低於 0.3 秒）。
* 內建高精度 SciPy/NumPy 頻譜能量暫態鼓點檢測演算法，精準鎖定樂曲重拍與 BPM。
* **高潮靶向對齊**：自動尋找 BGM 的高能量高潮段落，並將其起點與影片時間軸起點（`86400`）完美鎖定。

### 4. 智能邊界安全防抖保護區 (Smart Padding Margin)
* 內建 **15% 首尾安全屏蔽帶 (15% Safety Margin Guard)**：裁切時自動避開剛按下錄影鍵和準備停止錄影時的前後 15% 高晃動高危區間，強制從剩餘 70% 的黃金中段 (Pristine Mid-section) 進行擷取。

### 5. 動態滑動光流平穩度評估 (Rolling Motion Stability Scanner)
* 利用 OpenCV 在記憶體中將畫面降採樣 99% 至 `160x90` 縮圖（過濾風吹秀髮等細微雜訊，專注於相機宏觀運動），搭配 `downsample_step = 6` 跳幀前向連續解碼（比尋軌 seek 解碼快 **5.2 倍**）。
* 滑動分析全片運動能量方差 $\text{Var}(M[s : s+D])$。方差極低代表均速平穩搖鏡或懸停；對超過安全閾值的突發劇烈手抖給予重罰，自動檢索出全片物理運動最流暢的黃金 In 點！

### 6. 運鏡反向防護算法 (Motion Vector Monotonicity & Direction Reversal Defense)
* **一維水平投影剖面互相關算法 (1D Horizontal Projection Profile Cross-Correlation)**：
  對灰階圖像沿垂直方向加總，得到代表畫面水平特徵剖面的一維數組 $P(x)$。藉由相鄰幀的一維滑動互相關，計算出 sub-pixel 級別的逐幀水平位移量 $dx(t)$。
* **方向單調性比例得分 (Monotonicity Ratio)**：
  $$\text{Monotonicity Ratio} = \frac{\max(\sum [dx > 0], \sum [dx < 0])}{\text{Total Active Frames}}$$
  若比例低於 **`90%`**（區間內有超過 10% 的時間在反向運鏡），系統會觸發 **`Reversal Blocking Penalty` 阻斷性重罰**，強制過濾掉任何含有運鏡方向逆轉的倒退、鐘擺搖晃段落，確保剪出來的鏡頭永遠是「單方向平滑運動」的電影感鏡頭！

### 7. 25 秒黃金商業規格與觀眾畫面精準控鏡
* **觀眾/空鏡畫面嚴格限制為恰好 2 鏡**：全片中的 `Wide` (全景觀眾/環境空鏡) 素材被嚴格控制在恰好 2 鏡，精準地分配在**全片第 1 鏡 (Setup 開場)** 以及 **全片最後 1 鏡 (Finale 謝幕與品牌 Logo)**，其餘中間段落 100% 聚焦模特走秀與造型工藝。
* **25秒零重複素材對齊**：在 25 秒商業廣告長度下，對拍降頻算法會精準生成 **35 個剪擊拍點**。由於我們擁有 36 個獨一無二的 CloseUp 與 Medium 素材，此時長剛好落在**零重複素材（Zero-Repetition）的數學甜點區**！

### 8. 全域運動特徵永久快取系統 (`.cv_profile_cache.json`)
* 將原先與「時長、拍點位置」綁定的快取，升級為與「檔案絕對路徑」綁定的 **全域特徵資料庫**，儲存整支影片逐幀的運動向量與平穩度曲線。
* 二次微調、重新剪接或改變時長時，系統直接從快取讀取特徵曲線，**全片 35 個鏡頭的二次編譯與剪輯可在 0.01 秒內瞬間完成**！

### 9. 雙品牌 Outro 結尾卡排版設計 (BC & Schwarzkopf Logos)
* 系統會自動在時間軸建立 **Video Track 2 與 Video Track 3**，並在 **Finale 謝幕段落（第 20.8 秒 / 86895 影格）** 自動寫入 **BC LOGO** 與 **施華蔻 LOGO**。
* **精準排版參數**：
  * 雙 Logo 縮放至精緻中尺寸：`Zoom = 0.35`
  * **BC Logo (V2)** 偏左排版：`Pan = -260.0`, `Tilt = -50.0`
  * **施華蔻 Logo (V3)** 偏右排版：`Pan = 260.0`, `Tilt = -50.0`
  呈現極具國際高階時尚感的雙品牌 side-by-side 定格包裝。

### 10. `math.ceil` 跨影格率無縫對齊與 API 座標系修正
* 採用向上取整動態補償公式，徹底解決 29.97 FPS 素材在 24.0 FPS 時間軸上因浮點數捨入造成的 1 影格黑格問題：
  $$\text{duration\_source} = \text{int}(\text{math.ceil}(\text{duration\_timeline} \times \frac{\text{src\_fps}}{\text{timeline\_fps}}))$$
* **座標系修正**：達芬奇的 `AddMarker` 接收相對座標，而 `GetMarkers` 返回絕對座標。系統自動對 Marker 執行「減去起點影格（`86400`）」運算，達成高精度對齊。

---

## 📂 專案工具箱目錄與使用指南

### 🎬 主編輯與編譯引擎 (Main Editors)
* **`run_event_highlight_edit.py`**：
  * **功能**：專屬商業秀場對拍剪接主引擎。包含 2-Beat 降頻、首尾防手震屏蔽、全域快取載入、動態導演手持斜切、自動音軌清理、BGM高潮剪接、V2/V3雙 Logo 側排與 Master 調色同步。
  * **執行**：`python run_event_highlight_edit.py`
* **`create_semantic_timeline.py`**：
  * **功能**：專門用於重建並還原 「Sam前導片_高能行銷版」 時間軸的腳本，同時自動產出毫秒級完美對齊的 SRT 字幕檔。
  * **執行**：`python create_semantic_timeline.py`
* **`run_cinematic_auto_edit.py`**：
  * **功能**：基於理想運動包絡線平滑渡演算法的 30 秒自動剪輯展示引擎。

### ⚡ 資料特徵與快取引擎 (Data Engines)
* **`pre_cache_profiles.py`**：
  * **功能**：**完全獨立於達芬奇運行**。可在背景極速前向解碼並提取整個影片資料夾下所有 65 個 raw 片段的全量運動、平穩度與方向單調性曲線，並持久化寫入本機快取資料庫 `.cv_profile_cache.json`，為剪接提供 0.01 秒的即時運算支援！
  * **執行**：`python pre_cache_profiles.py`

### 🔍 分析與診斷工具 (Analyzers & Diags)
* **`stability_analyzer_hyper_fast.py`**：
  * **功能**：單一影片平穩度滾動光流掃描分析器。
  * **執行**：`python stability_analyzer_hyper_fast.py`
* **`direction_stability_analyzer.py`**：
  * **功能**：單一影片水平投影剖面互相關方向單調性（有無反向運鏡）掃描器。
  * **執行**：`python direction_stability_analyzer.py`
* **`diag_timelines.py`**：
  * **功能**：快速診斷達芬奇專案中所有時間軸的軌道、片段數量，並印出當前 GUI 視窗中正開啟並獲得焦點的時間軸名稱。
  * **執行**：`python diag_timelines.py`

### 🧪 驗證與導入工具 (Validators & Loaders)
* **`inspect_nanqu_work.py`**：
  * **功能**：高精度時間軸實體數據驗證器。直接查詢達芬奇資料庫，輸出主軌影片數、Logo軌屬性（Pan/Zoom/Tilt）、無重複素材比例、以及 BGM 長度對齊。
  * **執行**：`python inspect_nanqu_work.py`
* **`reimport_assets.py`**：
  * **功能**：自動建立 Master 階層並檢查導入 Google Drive 原始影片與音樂素材至達芬奇 Media Pool 的防重複引流工具。
  * **執行**：`python reimport_assets.py`

---

## 🤖 AI Rhythmic Editor System Prompt (開機指令集)

當使用者（剪輯導演）向你（AI Assistant）發送以下形式的剪輯指令時：
> **「給我 {XX} 秒的影片，使用 {BGM名稱} 當作音樂，我的風格是 {風格語境}，把焦點放在 {焦點對象}」**

你必須立即扮演本套系統的**核心控制台**，讀取當前工作區的所有工具，並按照以下**標準作業程序 (SOP)** 自動執行：

### 📥 1. 語意解析與代碼映射
1. **設定影片總長度 (秒)**：
   * 將 `{XX}` 映射為實體變數 `MAX_DURATION_SEC = float({XX})`。
   * 時長換算影格數：`total_duration_frames = int(MAX_DURATION_SEC * fps)`。
2. **定位背景配樂 (BGM)**：
   * 在程式碼中尋找 `find_bgm(folder)`，將搜尋關鍵字改為 `"{BGM名稱}".lower()`。
3. **對齊風格與檢索焦點**：
   * 根據 `{風格語境}` 與 `{焦點對象}`，動態修改剪輯引擎中的 `semantic_prompts`（例如 `run_event_highlight_edit.py`），將其解耦並重寫為適用於任何主題的通用檢索詞（Generic Query Expansion）：
     * `"setup"` ➡️ `"{風格語境} {焦點對象} establishing opening setup background context"`
     * `"detail"` ➡️ `"{焦點對象} macro closeup details features focus craftsmanship"`
     * `"catwalk"` ➡️ `"{焦點對象} high energy dynamic action movement climax performance"`
     * `"finale"` ➡️ `"{焦點對象} grand finale outro logo branding packaging presentation end"`

### ⚡ 2. 快取與焦點預檢防禦 (GUI Sync Gotcha)
1. **特徵快取檢查**：
   * 檢查本機快取 `.cv_profile_cache.json` 是否已覆蓋所有候選影片。若有新影片或缺失，立即在背景呼叫 `python pre_cache_profiles.py`。
2. **GUI 焦點防錯鎖定**：
   * 執行 `python diag_timelines.py` 檢查達芬奇 GUI 當前聚焦的時間軸是否為目標時間軸。
   * **🚨 達芬奇 GUI 焦點天坑**：若當前聚焦的非目標時間軸，你必須暫停並發出警示：「**請在 Resolve 軟體中雙擊該時間軸，確保編輯頁聚焦此時間軸，避免將素材 append 到錯誤的分頁！**」

### 🚀 3. 修改代碼與執行編譯
1. 使用 `replace_file_content` 工具，精確修改 `run_event_highlight_edit.py` 中的時長限制、音樂搜尋關鍵字與 CLIP 語意 Prompt。
2. 在終端機中執行：
   ```powershell
   python run_event_highlight_edit.py
   ```
3. 監控執行狀態，確保返回 `🎉 All tasks completed beautifully!` 且 Exit code 為 0。

### 📊 4. 實體數據驗證與謙遜呈報
1. 執行實體數據驗證：
   ```powershell
   python C:\Users\larrywu\.gemini\antigravity\brain\878978ab-d3f5-418c-88e5-9314eb79fe0f\scratch\inspect_nanqu_work.py
   ```
2. 收集並彙整驗證數據：
   * 影片主軌總卡點片段數（如 25 cuts）。
   * 雙 Logo 置入影格起點與 `Zoom`/`Pan`/`Tilt` 物理屬性。
   * BGM 對齊百分比。
   * 100% 零重複素材驗證結果。
3. 以高度專業、清晰、謙遜的語調向使用者呈報，不得使用 "perfectly", "flawlessly" 等誇大詞彙。
